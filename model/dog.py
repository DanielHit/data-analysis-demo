class Dog:

    def __init__(self, name, age, want):
        self.name = name
        self.age = age

    def fuck(self):
        print self.name + str(self.age)

    def sing(self):
        print self.name + str(self.age)
